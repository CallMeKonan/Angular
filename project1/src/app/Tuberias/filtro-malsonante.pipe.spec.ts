import { FiltroMalsonantePipe } from './filtro-malsonante.pipe';

describe('FiltroMalsonantePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMalsonantePipe();
    expect(pipe).toBeTruthy();
  });
});
