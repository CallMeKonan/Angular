import { LightButtonDirective } from './light-button-directive.directive';

describe('LightButtonDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LightButtonDirective();
    expect(directive).toBeTruthy();
  });
});
