import { SwearWordPipePipe } from './swear-word-pipe.pipe';

describe('SwearWordPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SwearWordPipePipe();
    expect(pipe).toBeTruthy();
  });
});
